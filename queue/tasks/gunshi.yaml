task:
  task_id: gunshi_qc_200_7_2
  parent_cmd: cmd_200
  type: qc
  bloom_level: L4
  assigned_to: gunshi
  status: done  # PASS。8項目チェックリスト全項目独立再実行で一致(commit stat・journal3エントリ・verifiers.md180行削除の過不足なし・①②健在・dashboard pending0件・機構変更0件・見出し23→17・bats654件1FAIL(既知)SKIP0)。cmd_200工程7完全クローズ。
  timestamp: "2026-09-22T23:45:00"
  description: |
    【cmd_200 工程7-2 QC: 生きている条件式10件→2件への削減
    (ashigaru3、commit b4c9408)】

    一次資料: `queue/shogun_to_karo.yaml` id: cmd_200「━━━ 工程7 ━━━」節
    (訂正後の受け入れ条件「10件→2件(−8)」を正とせよ)。ashigaru3の報告:
    `queue/reports/ashigaru3_report.yaml`(task_id: subtask_200_7_2)。
    比較元: 貴殿自身のgunshi_200_7_1報告(発見場所の行番号一覧)。

    🔴🔴本工程は本日2度の over-deletion 事故(工程4の3ファイル誤削除・
    工程1のcli_adapter判定混同)の直接の再発リスク工程として将軍が
    名指しした箇所である。**独立再実行での裏取りを最優先せよ**
    (Independent Verification Rule Step C)。

    ■ 確認事項

    1. `git show --stat b4c9408`で変更ファイルが`mandate/verifiers.md`・
       `mandate/decisions_journal.md`の2件のみであることを確認せよ
       (家老の事前確認: +6/-0・+0/-180)。
    2. `git show b4c9408 -- mandate/decisions_journal.md`で追記された
       3エントリの内容が以下と一致するか確認せよ:
       (a) ⑧「完了ゲートenforce移行条件」二重計上の経緯(commit 80cdc30が
           既に該当節を削除済みだった事実)
       (b) ⑪「cmd_192 skill初回読込caveat」の正しい件数は**12件**
           (11件ではない)、「skillの実読込は一度も観測されなかった」の
           文言を含むこと
       (c) ③「工程別内訳(phase-breakdown)」の評価結果(unknown率53.5%)を
           理由に閉じる旨
    3. `git show b4c9408 -- mandate/verifiers.md`の削除内容(diff全体)を
       読み、削除された6見出しが③④⑤⑥⑨⑩に過不足なく対応し、かつ
       **削除範囲が見出し+本文段落のみ**(前後の無関係な内容を巻き込んで
       いないか)を確認せよ。特に:
       - ⑩条件式(c)削除後、親見出し「## 状態を持つ機構の三層構成則
         (cmd_194・Q50(b))」(517-526行目相当)が**残置**されているか
       - ④条件式(a)削除後、次に来る「### 条件式(b)」(①・残す2件)が
         **無傷**か
    4. `grep -n "条件式(b)\|🎯 真陽性実証" mandate/verifiers.md dashboard.md`
       を独立再実行し、①②(残す2件)が両ファイルに存在することを確認せよ。
    5. `dashboard.md`(git非追跡)から`| cmd_147 |`・`| cmd_192 |`で始まる
       行が0件になっていることを`grep -c "^| cmd_147 \|^| cmd_192 " dashboard.md`
       で確認せよ。またヘッダー行・「解除済み事例」表・
       「🎯 真陽性実証」節が無傷であることも目視確認せよ。
    6. 🔴**条件式が参照する機構・ログ・スクリプトへの変更が0件であること**
       (今回の削除範囲との混同がないか)を`git status --short -- scripts/
       lib/ config/settings.yaml`で確認せよ(差分が出れば即FAIL)。
    7. `grep -c "^## \|^### " mandate/verifiers.md`で見出し数が
       23→17(−6)になっていることを独立再実行で確認せよ。
    8. `bats tests/unit/*.bats`を実行し、本工程がテストに影響していない
       こと(FAIL/SKIP件数が工程4是正時点の値から変化していないこと)を
       確認せよ。

    ■ 完了後
    bash /home/nishikawa/projects/multi-agent-shogun/scripts/inbox_write.sh karo \
      "gunshi_qc_200_7_2完了: <PASS/FAIL>。<1行要約>。" \
      report_received gunshi --cmd_id=cmd_200 --task_id=gunshi_qc_200_7_2 --qc_result=<pass/fail>
