task:
  task_id: gunshi_qc_203_2
  parent_cmd: cmd_203
  type: qc
  bloom_level: L4
  assigned_to: gunshi
  status: done  # PASS。commit stat/diff一致・実挙動独立再現(a穴A是正/b4パターン回帰無し/c警告非破壊性)・フルbats654件0 not ok・get_in_flight_tasks_with_agent残留0件・git status clean、全6項目一致。工程2完全クローズ。
  timestamp: "2026-09-23T01:40:00"
  description: |
    【cmd_203 工程2 QC: 穴A是正(clear_command→assigned)+task_id警告追加
    (ashigaru1、commit b11623c)——全エージェント下達の本番経路への
    変更・最重要注意工程】

    一次資料: `queue/shogun_to_karo.yaml` id: cmd_203「━━━ 工程2 ━━━」節、
    将軍裁定(2026-09-23T01:20:52④)。ashigaru1の報告:
    `queue/reports/ashigaru1_report.yaml`(task_id: subtask_203_2)。

    🔴🔴**極めて重要**: `scripts/inbox_write.sh`は全エージェントへの下達が
    通る唯一の本番経路である。誤りがあれば全軍の指示配達が止まる。

    ■ 変更内容(確認済みの前提・結論のみ検証すればよい)
    - 69-74行目付近: `clear_command`のTYPE分岐を`_TIMING_EVENT="assigned"`
      へ変更(従来は空文字で記帳されなかった=穴A)
    - 207-209行目付近: `_TIMING_TASK_ID`が空の場合の警告echoを追加
      (既存の`_TIMING_CMD_ID`版と同型、fail-loudのみ・配達停止ロジックなし)

    ■ 確認事項

    1. `git show --stat b11623c`で変更が`scripts/inbox_write.sh`1ファイル
       のみ(+4/-1)であることを確認せよ。
    2. `git show b11623c`のdiff内容を読み、変更が上記2箇所のみ(意図通り
       最小)であることを確認せよ。
    3. 🔴**実挙動の独立再現(最優先)**: ashigaru1と同様、本番agentの
       inboxへ直接影響しない隔離環境(`TEST_TMP`方式、`tests/unit/
       test_urgent_escalation_e2e.bats`等の既存隔離パターンに倣う。
       `TIMING_EVENT_SYNTHETIC=1`宣言を忘れるな)を用いて、貴殿自身の手で
       以下を再現せよ:
       (a) `clear_command`型(`--redo_of`無し)の下達 → `assigned`イベントが
           記帳されることを確認(是正前後の対比)
       (b) regression確認: `task_assigned`(redo無/有)・`clear_command`
           (redo有)・`report_received`の4パターンが従来どおり動作する
           ことを確認
       (c) `--task_id`省略時に新規警告が出力され、かつ配達(inboxへの
           実書き込み)が正常に成功する(止まらない)ことを確認
    4. `bats tests/unit/*.bats`を独立再実行し、not ok集合が是正前
       (TC-FAM-001/007/008の3件のみ、既知flaky)から変化していないことを
       確認せよ。
    5. `bash -c 'source lib/inflight_tasks.sh; get_in_flight_tasks_with_agent'`
       を実行し、完了済みタスクが1件も残っていないことを確認せよ
       (cmd_203全体の受け入れ条件の1つ)。
    6. `git status --porcelain`で`scripts/inbox_write.sh`に差分が残って
       いないことを確認せよ。

    ■ 完了後
    bash /home/nishikawa/projects/multi-agent-shogun/scripts/inbox_write.sh karo \
      "gunshi_qc_203_2完了: <PASS/FAIL>。<1行要約>。" \
      report_received gunshi --cmd_id=cmd_203 --task_id=gunshi_qc_203_2 --qc_result=<pass/fail>
